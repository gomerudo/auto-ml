"""Save the basic (unit) testing required for the application.

The auto-ml tool requires some modules to strictly work correctly in order to
ensure a correct behavior. This module contains one test module per each of the
main components.
"""
